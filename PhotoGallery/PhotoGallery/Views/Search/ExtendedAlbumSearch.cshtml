@model ExtendedAlbumSearchViewModel
@{
    ViewBag.Title = "ExtendedAlbumSearch";
}

<h2>Extended Album Search</h2>

<div class="container">
    <div class="col-md-4">
        <div class="row">
            @using (Html.BeginForm("ExtendedAlbumSearch", "Search", FormMethod.Post))
            {
                <div class="panel-body">
                    @foreach (var property in ViewData.ModelMetadata.Properties)
                    {
                        <div class="form-group">
                            <label>@(property.DisplayName ?? property.PropertyName)</label>
                            @Html.TextBox(property.PropertyName, null,
                            new { @class = "form-control" })
                            @Html.ValidationMessage(property.PropertyName)
                        </div>
                    }
                    <input type="submit" class="btn btn-default" value="Search for album" />
                </div>
            }
        </div>
    </div>
</div>